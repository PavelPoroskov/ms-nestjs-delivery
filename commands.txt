//generate with @nestjs/cli@12 with ESM Modules
nest new uber-services
  choose: npm
  choose: auto-instrumented observability (@nestjs/observe) No
  choose: ESM Modules (with vitest)

cd uber-services
rm -rf .git // use .git in root folder

cd uber-services
nest generate app logging
nest generate app rider

cd uber-services/apps/logging/src
nest g module rider-coordinates
nest g controller rider-coordinates
nest g service rider-coordinates

cd uber-services
npm i mongoose @nestjs/mongoose
npm i class-transformer class-validator
npm i @nestjs/config
npm i @nestjs/microservices


// STARTING
cp uber-services0/apps/logging/.env-example uber-services0/apps/logging/.env
// fill username and password

// terminal 1
cd uber-services/apps/logging
source .env && podman-compose up

// terminal 2
nest start logging
    // nest start logging
    // Error: The "@rspack/core" package is required when using the rspack compiler but could not be found. Please install it:
    npm install --save-dev @rspack/core webpack-node-externals tsconfig-paths-webpack-plugin

    // nest start logging
    // Error Authentication failed
    // rm old podman container
    // rm old podman volume (mongodb with old password)


// terminal 3
// TESTING
curl -H "Content-Type: application/json" \
  --request POST \
  --data '{"lat":11.11, "lng": 22.22, "rider":"Bob"}' \
  http://127.0.0.1:3002/rider-coordinates

curl -H "Content-Type: application/json" http://127.0.0.1:3002/rider-coordinates


// STARTING
// terminal 1
cd uber-services/apps/logging
source .env && podman-compose up
// terminal 2
cd uber-services
nest start rider
// terminal 3
cd uber-services
nest start logging
// terminal 3
curl -H "Content-Type: application/json" \
  --request POST \
  --data '{"lat":11.11, "lng": 22.22, "rider":"Bob"}' \
  http://127.0.0.1:3002/rider-coordinates
curl -H "Content-Type: application/json" http://127.0.0.1:3002/rider-coordinates/Bob
